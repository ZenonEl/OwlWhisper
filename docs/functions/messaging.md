# 📨 **Отправка сообщений**

**Последнее обновление:** 23 августа 2025

⚠️ **ВНИМАНИЕ: Эти функции будут изменены в будущем!**

Функции для отправки текстовых сообщений. В будущем будут переделаны на универсальные методы для отправки любых данных.

## 📋 **Функции**

### **`SendMessage(text)`**

**Описание:** Отправляет текстовое сообщение всем подключенным пирам (broadcast).

**Сигнатура:**
```c
int SendMessage(char* text);
```

**Параметры:**
- `text` (char*) - текст сообщения для отправки

**Возвращает:**
- `0` - успешная отправка
- `-1` - ошибка отправки

**Пример использования:**
```python
import ctypes

# Настройка типа возвращаемого значения
owlwhisper.SendMessage.restype = ctypes.c_int

# Отправка сообщения всем подключенным пирам
message = "Привет, мир!".encode('utf-8')
result = owlwhisper.SendMessage(message)
if result == 0:
    print("✅ Сообщение отправлено всем пирам")
else:
    print("❌ Ошибка отправки сообщения")
```

**Что происходит при отправке:**
1. Сообщение сериализуется в Protobuf формат
2. Открываются потоки ко всем подключенным пирам
3. Данные отправляются через libp2p потоки
4. Потоки автоматически закрываются после отправки

**Важные замечания:**
- ⚠️ **Будет изменено в будущем** - переделано на `SendData(data)` для любых данных
- ⚠️ **Отправляется только текст** - не поддерживает бинарные данные
- 💡 **Используйте для простых текстовых сообщений** в MVP
- 🔄 **Broadcast всем пирам** - нет выбора конкретного получателя

---

### **`SendMessageToPeer(peerID, text)`**

**Описание:** Отправляет текстовое сообщение конкретному пиру.

**Сигнатура:**
```c
int SendMessageToPeer(char* peerID, char* text);
```

**Параметры:**
- `peerID` (char*) - Peer ID получателя
- `text` (char*) - текст сообщения для отправки

**Возвращает:**
- `0` - успешная отправка
- `-1` - ошибка отправки

**Пример использования:**
```python
import ctypes

# Настройка типов параметров и возвращаемого значения
owlwhisper.SendMessageToPeer.argtypes = [ctypes.c_char_p, ctypes.c_char_p]
owlwhisper.SendMessageToPeer.restype = ctypes.c_int

# Отправка сообщения конкретному пиру
peer_id = "12D3KooW...".encode('utf-8')
message = "Привет, конкретный пир!".encode('utf-8')
result = owlwhisper.SendMessageToPeer(peer_id, message)
if result == 0:
    print("✅ Сообщение отправлено конкретному пиру")
else:
    print("❌ Ошибка отправки сообщения")
```

**Что происходит при отправке:**
1. Проверяется существование пира в сети
2. Открывается поток к указанному пиру
3. Сообщение сериализуется в Protobuf формат
4. Данные отправляются через libp2p поток
5. Поток автоматически закрывается после отправки

**Важные замечания:**
- ⚠️ **Будет изменено в будущем** - переделано на `SendDataToPeer(peerID, data)` для любых данных
- ⚠️ **Отправляется только текст** - не поддерживает бинарные данные
- 💡 **Используйте для адресных сообщений** конкретному получателю
- 🔍 **Пир должен быть подключен** - иначе отправка не удастся

## 🔄 **Сценарии использования**

### **Отправка уведомлений всем:**

```python
def broadcast_notification(message_text):
    """Отправка уведомления всем подключенным пирам"""
    try:
        message = message_text.encode('utf-8')
        result = owlwhisper.SendMessage(message)
        
        if result == 0:
            print(f"✅ Уведомление отправлено всем: {message_text}")
            return True
        else:
            print(f"❌ Ошибка отправки уведомления: {result}")
            return False
            
    except Exception as e:
        print(f"❌ Исключение при отправке: {e}")
        return False

# Пример использования
broadcast_notification("Система будет перезагружена через 5 минут")
```

### **Отправка личных сообщений:**

```python
def send_personal_message(peer_id, message_text):
    """Отправка личного сообщения конкретному пиру"""
    try:
        peer_id_bytes = peer_id.encode('utf-8')
        message_bytes = message_text.encode('utf-8')
        
        result = owlwhisper.SendMessageToPeer(peer_id_bytes, message_bytes)
        
        if result == 0:
            print(f"✅ Личное сообщение отправлено {peer_id}: {message_text}")
            return True
        else:
            print(f"❌ Ошибка отправки личного сообщения: {result}")
            return False
            
    except Exception as e:
        print(f"❌ Исключение при отправке: {e}")
        return False

# Пример использования
send_personal_message("12D3KooW...", "Привет! Как дела?")
```

### **Отправка системных команд:**

```python
def send_system_command(peer_id, command):
    """Отправка системной команды пиру"""
    try:
        # Формируем системную команду
        system_message = f"SYSTEM_CMD:{command}"
        
        peer_id_bytes = peer_id.encode('utf-8')
        message_bytes = system_message.encode('utf-8')
        
        result = owlwhisper.SendMessageToPeer(peer_id_bytes, message_bytes)
        
        if result == 0:
            print(f"✅ Системная команда отправлена {peer_id}: {command}")
            return True
        else:
            print(f"❌ Ошибка отправки команды: {result}")
            return False
            
    except Exception as e:
        print(f"❌ Исключение при отправке команды: {e}")
        return False

# Пример использования
send_system_command("12D3KooW...", "RESTART_SERVICE")
```

### **Массовая рассылка:**

```python
def mass_message_send(peer_ids, message_text):
    """Массовая рассылка сообщений списку пиров"""
    try:
        success_count = 0
        total_count = len(peer_ids)
        
        for peer_id in peer_ids:
            if send_personal_message(peer_id, message_text):
                success_count += 1
        
        print(f"✅ Массовая рассылка завершена: {success_count}/{total_count} успешно")
        return success_count, total_count
        
    except Exception as e:
        print(f"❌ Ошибка массовой рассылки: {e}")
        return 0, total_count

# Пример использования
peer_list = ["12D3KooW...", "12D3KooW...", "12D3KooW..."]
mass_message_send(peer_list, "Важное объявление для всех участников!")
```

## ⚠️ **Важные замечания**

### **Ограничения текущей версии:**
- **Только текстовые сообщения** - не поддерживает бинарные данные
- **Только UTF-8 кодировка** - другие кодировки не поддерживаются
- **Максимальная длина** - ограничена размером libp2p потока

### **Будущие изменения:**
- **`SendMessage(text)` → `SendData(data)`** - универсальная отправка любых данных
- **`SendMessageToPeer(peerID, text)` → `SendDataToPeer(peerID, data)`** - универсальная адресная отправка
- **Поддержка Protobuf** - структурированные сообщения вместо простого текста
- **Поддержка метаданных** - заголовки, типы сообщений, приоритеты

### **Рекомендации для MVP:**
- **Используйте текущие функции** для простых текстовых сообщений
- **Готовьтесь к миграции** на новые универсальные методы
- **Не создавайте сложную логику** вокруг текстовых сообщений
- **Планируйте переход** на структурированные данные

### **Производительность:**
- **Отправка занимает 10-100 мс** в зависимости от размера сети
- **Broadcast медленнее** - отправка ко всем пирам последовательно
- **Потоки создаются динамически** - нет пула соединений
- **Автоматическое закрытие** - ресурсы освобождаются после отправки

### **Надежность:**
- **Нет гарантии доставки** - сообщения могут потеряться
- **Нет подтверждения получения** - отправитель не знает о доставке
- **Автоматический retry отсутствует** - нужно реализовывать вручную
- **Проверка подключения** - пир должен быть онлайн

## 🔗 **Связанные функции**

- [Система событий](../functions/events-system.md) - получение входящих сообщений через `GetNextEvent()`
- [Поиск пиров](../functions/peer-discovery.md) - поиск получателей для отправки
- [Мониторинг сети](../functions/network-monitoring.md) - проверка состояния соединений

---

**Последнее обновление:** 23 августа 2025  
**Автор:** Core Development Team 