# ⚠️ **Устаревшие функции**

**Последнее обновление:** 23 августа 2025

Функции, которые будут удалены или изменены в будущих версиях Owl Whisper Core.

## 🗑️ **Функции для удаления**

### **`GetChatHistory(peerID)`**

**Статус:** 🗑️ **Будет удален**

**Причина удаления:** GUI сам управляет историей сообщений, Core не должен хранить пользовательские данные.

**Текущая сигнатура:**
```c
char* GetChatHistory(char* peerID);
```

**Что использовать вместо:**
- **GUI сам хранит историю** в локальной базе данных
- **Система событий** через `GetNextEvent()` для получения новых сообщений
- **Локальное хранение** сообщений в SQLite/файлах

**План миграции:**
1. **Этап 1:** Функция помечена как deprecated
2. **Этап 2:** Функция возвращает пустые данные
3. **Этап 3:** Функция полностью удалена из API

---

### **`GetChatHistoryLimit(peerID, limit)`**

**Статус:** 🗑️ **Будет удален**

**Причина удаления:** Аналогично `GetChatHistory` - GUI сам управляет историей.

**Текущая сигнатура:**
```c
char* GetChatHistoryLimit(char* peerID, int limit);
```

**Что использовать вместо:**
- **GUI сам реализует пагинацию** истории сообщений
- **Локальные запросы** к базе данных с LIMIT
- **Кэширование** часто запрашиваемых сообщений

---

### **`EnableAutoReconnect()`**

**Статус:** 🗑️ **Будет удален**

**Причина удаления:** ConnectionManager с защищенными пирами делает эту функцию избыточной.

**Текущая сигнатура:**
```c
int EnableAutoReconnect();
```

**Что использовать вместо:**
- **`AddProtectedPeer(peerID)`** - добавление пира в защищенные
- **ConnectionManager** автоматически управляет переподключением
- **Встроенная логика** переподключения в Core

**План миграции:**
1. **Этап 1:** Функция помечена как deprecated
2. **Этап 2:** Функция не выполняет действий
3. **Этап 3:** Функция полностью удалена из API

---

### **`DisableAutoReconnect()`**

**Статус:** 🗑️ **Будет удален**

**Причина удаления:** Аналогично `EnableAutoReconnect`.

**Текущая сигнатура:**
```c
int DisableAutoReconnect();
```

**Что использовать вместо:**
- **`RemoveProtectedPeer(peerID)`** - удаление пира из защищенных
- **ConnectionManager** автоматически прекращает переподключение

---

### **`IsAutoReconnectEnabled()`**

**Статус:** 🗑️ **Будет удален**

**Причина удаления:** Аналогично `EnableAutoReconnect`.

**Текущая сигнатура:**
```c
int IsAutoReconnectEnabled();
```

**Что использовать вместо:**
- **`GetProtectedPeers()`** - проверка защищенных пиров
- **Локальное отслеживание** состояния в GUI

---

### **`GetReconnectAttempts(peerID)`**

**Статус:** 🗑️ **Будет удален**

**Причина удаления:** Аналогично `EnableAutoReconnect`.

**Текущая сигнатура:**
```c
int GetReconnectAttempts(char* peerID);
```

**Что использовать вместо:**
- **`GetConnectionQuality(peerID)`** - общая информация о соединении
- **Локальное отслеживание** попыток в GUI

## 🔄 **Функции для изменения**

### **`SendMessage(text)`**

**Статус:** 🔄 **Будет изменен**

**Планируемые изменения:**
- **Параметр:** `char* text` → `char* data`
- **Название:** `SendMessage` → `SendData`
- **Функциональность:** Только текст → Любые данные

**Новая сигнатура:**
```c
int SendData(char* data);
```

**Что изменится:**
1. **Поддержка бинарных данных** вместо только текста
2. **Универсальность** - можно отправлять любые типы данных
3. **Совместимость с Protobuf** - структурированные сообщения
4. **Метаданные** - заголовки, типы, приоритеты

**План миграции:**
1. **Этап 1:** Добавлена новая функция `SendData`
2. **Этап 2:** `SendMessage` помечена как deprecated
3. **Этап 3:** `SendMessage` удалена, остается только `SendData`

---

### **`SendMessageToPeer(peerID, text)`**

**Статус:** 🔄 **Будет изменен**

**Планируемые изменения:**
- **Параметры:** `(peerID, text)` → `(peerID, data)`
- **Название:** `SendMessageToPeer` → `SendDataToPeer`
- **Функциональность:** Только текст → Любые данные

**Новая сигнатура:**
```c
int SendDataToPeer(char* peerID, char* data);
```

**Что изменится:**
1. **Поддержка бинарных данных** вместо только текста
2. **Универсальность** - можно отправлять любые типы данных
3. **Совместимость с Protobuf** - структурированные сообщения
4. **Метаданные** - заголовки, типы, приоритеты

**План миграции:**
1. **Этап 1:** Добавлена новая функция `SendDataToPeer`
2. **Этап 2:** `SendMessageToPeer` помечена как deprecated
3. **Этап 3:** `SendMessageToPeer` удалена, остается только `SendDataToPeer`

## 📅 **План миграции**

### **Версия 1.5 (Следующий релиз):**
- ✅ Добавлены новые универсальные функции `SendData*`
- ⚠️ Функции `SendMessage*` помечены как deprecated
- ⚠️ Функции `GetChatHistory*` помечены как deprecated
- ⚠️ Функции `EnableAutoReconnect*` помечены как deprecated

### **Версия 2.0 (Основной релиз):**
- ❌ Функции `SendMessage*` удалены
- ❌ Функции `GetChatHistory*` удалены
- ❌ Функции `EnableAutoReconnect*` удалены
- ✅ Остаются только новые универсальные функции

### **Версия 1.4 (Текущая):**
- ✅ Все функции работают как обычно
- 📝 Документация обновлена с предупреждениями
- 🔄 Подготовка к миграции

## 💡 **Рекомендации для разработчиков**

### **Для MVP (текущая версия):**
- **Используйте существующие функции** для быстрого прототипирования
- **Не создавайте сложную логику** вокруг текстовых сообщений
- **Готовьтесь к миграции** на новые универсальные методы

### **Для долгосрочных проектов:**
- **Начните использовать новые функции** `SendData*` сразу после их появления
- **Планируйте архитектуру** для работы с бинарными данными
- **Изучайте Protobuf** для структурированных сообщений

### **Для миграции существующего кода:**
- **Постепенно заменяйте** `SendMessage*` на `SendData*`
- **Обновляйте обработчики** для работы с бинарными данными
- **Тестируйте совместимость** с новыми функциями

## 🔗 **Связанная документация**

- [Отправка сообщений](../functions/messaging.md) - текущие функции с предупреждениями
- [Система событий](../functions/events-system.md) - получение сообщений через события
- [Управление Core](../functions/core-management.md) - жизненный цикл Core

---

**Последнее обновление:** 23 августа 2025  
**Автор:** Core Development Team 